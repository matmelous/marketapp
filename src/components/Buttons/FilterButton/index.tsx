import { View, Text } from "react-native"
import { 
  Container,
  BoxContent,
} from "./styled"

export const FilterButton = () => {
  return(
    <Container>
      <BoxContent>
        
      </BoxContent>
    </Container>
  )
}