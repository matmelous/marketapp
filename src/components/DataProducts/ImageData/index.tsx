
import { Content, Container, Title} from "./styled"

export const ImageData = ({label}) => {
  return(
   <Content>
    <Container>
    <Title>
        {label}
    </Title>
    </Container>
   </Content>
  
  )
}